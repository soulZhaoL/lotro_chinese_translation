# 版本迭代方案说明（已升级）

原方案基于 xlsx 进行新老版本比对，存在以下关键风险：

1. 仅使用 `fid+textId+文本长度` 会误判（长度相同但文本内容变化时会错误复用译文）。
2. 版本切换依赖“主表改名”会影响关联表（认领/锁定/变更历史）。
3. 缺少版本维度，不利于回滚、审计和历史追踪。

已升级为数据库版本化方案，完整步骤见：

- `docs/db_version_iteration_runbook.md`

后续请以该运行手册为唯一执行依据。
