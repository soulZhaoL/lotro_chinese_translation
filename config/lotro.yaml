database:
  dsn: "${LOTRO_DATABASE_DSN}"

auth:
  hash_algorithm: "sha256"
  salt_bytes: 16
  token_secret: "${LOTRO_TOKEN_SECRET}"
  token_ttl_seconds: 86400

pagination:
  default_page_size: 20
  max_page_size: 200

locks:
  default_ttl_seconds: 1800
