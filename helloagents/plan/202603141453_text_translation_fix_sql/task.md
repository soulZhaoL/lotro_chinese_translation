# 任务清单: 旧版导入异常译文修复 SQL 生成

目录: `helloagents/plan/202603141453_text_translation_fix_sql/`

---

## 1. 协议解析与修复脚本
- [√] 1.1 在 `tools/fix_textid/` 下新增修复脚本，参照 `xlsx_to_insert_segmented.py` 的分段解析逻辑，解析 `work_text/U46.1.xlsx` 为 `(fid, textId)` 索引，验证 why.md#需求-按现行协议规则重建正确译文-场景-解析复合协议文本
- [√] 1.2 在修复脚本中实现两份差异样本读取、去重与一致性校验，验证 why.md#需求-仅修复差异样本命中的数据库记录-场景-合并两份差异样本
- [√] 1.3 在修复脚本中实现“样本记录 + 新版解析记录”精确匹配逻辑，并对原文一致性做硬校验，验证 why.md#需求-输出可审计的修复结果-场景-交付修复脚本

## 2. SQL 生成与审计结果
- [√] 2.1 在修复脚本中生成 `tmp_fix_text_main_translated_text.sql`，使用 `fid + textId + 样本错误译文` 作为更新保护条件，验证 why.md#需求-防止覆盖后来人工修正的数据-场景-数据库当前内容已不同于样本错误译文
- [√] 2.2 在修复脚本中生成 `tmp_fix_text_main_translated_text_report.csv` 与 `tmp_fix_text_main_translated_text_abnormal.csv`，输出命中、未匹配、解析异常等结果，验证 why.md#需求-输出可审计的修复结果-场景-交付修复脚本

## 3. 安全检查
- [√] 3.1 执行安全检查，确认脚本未另写一套协议正则、未使用默认配置 fallback、未对异常记录静默跳过

## 4. 文档更新
- [√] 4.1 将本次修复工具和产物说明同步到 `helloagents/wiki/modules/validation.md` 或相关模块文档

## 5. 测试
- [√] 5.1 运行脚本并基于样本中的典型记录检查生成 SQL，验证点: 复合 `textId` 匹配正确、重叠样本去重正确、保护条件正确
- [√] 5.2 抽样核对报告文件中的异常项，验证点: 未匹配/冲突项被显式列出，没有被静默吞掉

---

## 任务状态符号
- `[ ]` 待执行
- `[√]` 已完成
- `[X]` 执行失败
- `[-]` 已跳过
- `[?]` 待确认
